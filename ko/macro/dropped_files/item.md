# Item 속성 (DroppedFiles 컬렉션)

지정된 인덱스의 삭제된 파일을 위해 파일 이름을 검색합니다.

## 

### \[JavaScript\]

```
str = DroppedFiles.Item( Index );
```

### \[VBScript\]

```
str = DroppedFiles.Item( Index )
```

## 매개 변수

_Index_

삭제된 파일의 인덱스를 1로 시작하는 정수로 지정합니다.

## 버전

엠에디터 프로페셔널 버전 8.00 이상에서만 지원됩니다.
