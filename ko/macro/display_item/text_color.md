# TextColor 속성 (DisplayItem 개체)

개체의 텍스트 색상을 지정합니다.

## 

### \[JavaScript\]

```
rgb = item.TextColor;
item.TextColor = rgb;
```

### \[VBScript\]

```
rgb = item.TextColor;
item.TextColor = rgb;
```

## 버전

엠에디터 프로페셔널 버전 7.00 이상에서만 지원됩니다.
