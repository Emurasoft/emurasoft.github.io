# 선택된 문자열 얻기 (Ʃ�丮��)

선택된 문자열을 얻으려면, [Text 속성](../selection/selection_text) 을 사용합니다.

다음의 예문은 선택된 문자열을 얻어 표시하는 방법을 나타냅니다.

## \[JavaScript\]

```
str = document.selection.Text;
alert( str );
```

## \[VBScript\]

```
str = document.selection.Text
alert str
```
