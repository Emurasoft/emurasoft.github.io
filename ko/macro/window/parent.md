# Parent 속성 (Window ��ü)

상위 window 개체를 반환합니다.

#### \[JavaScript\]

_wnd_ = wnd. **Parent**;

wnd. **Parent** = _wnd_;

#### \[VBScript\]

_wnd_ = wnd. **Parent**

wnd. **Parent** = _wnd_

## 버전

엠에디터 프로페셔널 버전 7.00 이상에서만 지원됩니다.
