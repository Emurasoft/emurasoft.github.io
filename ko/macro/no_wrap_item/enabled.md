# Enabled 속성 (NoWrapItem ��ü)

개체를 활성화 할지 여부를 나타내는 플래그를 지정합니다.

## 

### \[JavaScript\]

```
b = item.Enabled;
item.Enabled = b;
```

### \[VBScript\]

```
b = item.Enabled
item.Enabled = b
```

## 버전

엠에디터 프로페셔널 버전 7.00 이상에서만 지원됩니다.
