# SaveCloseAll 方法 (Editor ��H)

儲存并關閉所有打開的檔案。

#### \[JavaScript\]

editor. **SaveCloseAll**();

#### \[VBScript\]

editor. **SaveCloseAll**

## 版本

支持 EmEditor 4.00 或之後的版本。