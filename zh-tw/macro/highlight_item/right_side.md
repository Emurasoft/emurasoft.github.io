# RightSide 屬性 (HighlightItem 對象)

與組態屬性中 [**亮顯 (1)** 頁面](../../dlg/properties/highlight1/index) 上的 **亮顯到選定單字右側** 核取方塊相對應。

## 

### \[JavaScript\]

```
b = item.RightSide;
item.RightSide = b;
```

### \[VBScript\]

```
b = item.RightSide
item.RightSide = b
```

## 版本

支持 EmEditor 7.00 或之後的版本。
