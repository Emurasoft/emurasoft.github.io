# End 屬性 (Filter 對象)

指定你要搜索的文字的末尾欄的索引 (邏輯字元數)。指定 0 如果你想要搜索剩余的文字。要啟用這個欄位， _Column_ 屬性必須是 -1 。

## 

### \[JavaScript\]

```
n = item.End;
item.End = n;
```

### \[VBScript\]

```
n = item.End
item.End = n
```

## 版本

支持 EmEditor Professional Version 16.0 或之後的版本。
