# Parent 屬性 (Window ��H)

返回父視窗對象。

#### \[JavaScript\]

_wnd_ = wnd. **Parent**;

wnd. **Parent** = _wnd_;

#### \[VBScript\]

_wnd_ = wnd. **Parent**

wnd. **Parent** = _wnd_

## 版本

支持 EmEditor 7.00 或之後的版本。
