# CurrentDirectory 屬性 (OutputBar 對象)

為輸出列設置當前目錄。這個信息是必需的如果文本包含只能從當前目錄跳至的相對路徑。

## 

### \[JavaScript\]

```
OutputBar.CurrentDirectory = szCurrDir;
```

### \[VBScript\]

```
OutputBar.CurrentDirectory = szCurrDir
```

## 版本

支持 EmEditor 7.00 或之後的版本。
