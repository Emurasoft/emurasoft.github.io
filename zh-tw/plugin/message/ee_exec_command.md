# EE\_EXEC\_COMMAND

執行一個指定的命令。您能明確地發送該消息或用 [Editor\_ExecCommand](../macro/editor_execcommand) 內嵌函式。

EE\_EXEC\_COMMAND

wParam = (WPARAM) (UINT) nCmdID;

lParam = 0;

## 參數

_nCmdID_

要執行的命令的識別符。請檢視
[命令 ID](../cmdid/index)。

## 返回值

不使用返回值。
