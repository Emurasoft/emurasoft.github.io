# 優化打開大檔案

EmEditor 現在能讓您只用一點內存就輕松地打開有 16 TB 大或 1.09 兆行的檔案。新版本特別優化了打開大檔案的速度，所以它比前幾版都快。EmEditor 的多線程設計讓您在打開一個大型檔案的同時還能檢視其他文檔。另外，當您在進行耗時的操作，比如文字編輯，儲存，搜尋，取代，插入和刪除時，EmEditor 會顯示一個狀態視窗，讓您能隨時記錄并取消這些操作。即使只是處理一些小檔案，新版本也大大提高了編輯器整體的文字處理速度，例如在進行打開檔案，編輯，儲存，搜尋文字和取代等等操作時。
