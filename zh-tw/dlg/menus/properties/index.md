# 「功能表屬性」對話方塊

這個對話方塊會顯示當
**「屬性」** 按鈕在 [**自訂功能表** 對話方塊](../index) 上被點擊時。

### 「命令」單選按鈕

指定一個命令功能表。

### 「快顯」單選按鈕

指定一個快顯功能表作為功能表資料夾。

### 「分隔符」單選按鈕

指定一個分隔符分隔命令。

### 類別清單方塊

命令的可用類別。

### 命令清單方塊

被選取類別中的可用命令。

### 描述文字

顯示描述被選取命令。

### 名稱文字方塊

指定一個功能表名稱。使用 & 標記來指定一個快速鍵。

