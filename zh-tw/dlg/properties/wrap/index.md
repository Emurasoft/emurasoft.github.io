# 「換行」頁面

**換行** 頁面讓您能設置與換行相關的屬性。

## 「自動換行」核取方塊

啟用自動換行。

## 「啟用標記后自動換行」核取方塊

啟用標記后自動換行。

## 「將指定字元換行」核取方塊

按 \[行首處不允許\] 和 \[行尾處不允許\] 文字方塊中的設定換行。

## 「行首處不允許」文字方塊

不允許這些字元在行首換行。

## 「行尾處不允許」文字方塊

不允許這些字元在行末換行。

## 「重設」按鈕

重設為預設設定。會顯示 [**重設** 對話方塊](../reset/index) 并讓您能從另一個組態複製設定。

