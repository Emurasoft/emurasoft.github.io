# FindWindowByID 메서드

창 식별자로 하위 Window 개체를 검색합니다.

#### \[JavaScript\]

wndChild = wnd. **FindWindowByID**( id );

#### \[VBScript\]

wndChild = wnd. **FindWindowByID**( id )

## 매개 변수

_id_

창 식별자를 지정합니다.

## 버전

엠에디터 프로페셔널 버전 7.00 이상에서만 지원됩니다.