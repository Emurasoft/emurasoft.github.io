# Load 메서드

구성을 로드합니다.

#### \[JavaScript\]

object. **Load**( \[ _strName_ \] );

#### \[VBScript\]

object. **Load** \[ _strName_ \]

## 매개 변수

_strName_

선택 사항입니다. 로드할 구성의 이름을 지정합니다. 생략된 경우, 이름이 변경되지 않습니다.

## 버전

엠에디터 프로페셔널 버전 7.00 이상에서만 지원됩니다.