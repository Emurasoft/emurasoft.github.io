# VisibleLinesAbove 属性

指定匹配行以上可见行的行数。

#### \[JavaScript\]

_n_ = list. **VisibleLinesAbove**;

list. **VisibleLinesAbove** = _n_;

#### \[VBScript\]

_n_ = list. **VisibleLinesAbove**

list. **VisibleLinesAbove** = _n_

## 版本

支持 EmEditor 16.2 或之后的版本。