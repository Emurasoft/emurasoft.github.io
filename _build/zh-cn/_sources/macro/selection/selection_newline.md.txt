# NewLine 方法

在光标处插入指定的新行数。

#### \[JavaScript\]

document.selection. **NewLine**( \[ _nCount_ \] );

#### \[VBScript\]

document.selection. **NewLine** \[ _nCount_ \]

## 参数

_nCount_

可选项。指定光标位置处的新行数。默认值是 1。

## 版本

支持 EmEditor 4.00 或之后的版本。