# Item 属性

为指定的索引检索 [FontItem 对象](../font_item/index)。

#### \[JavaScript\]

_item_ =
List. **Item**( _Index_ );

#### \[VBScript\]

_item_ =
List. **Item**( _Index_ )

## 参数

_Index_

把项目的索引指定为以 1 为基准的整数。

## 版本

支持 EmEditor 7.00 或之后的版本。