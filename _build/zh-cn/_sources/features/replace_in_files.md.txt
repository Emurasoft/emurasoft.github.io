# 在文件中替换

**“在文件中替换”** 功能是 EmEditor Professional 4.0 中的一个新功能。与 **“在文件中查找”** 类似，您可以用正则表达式搜索特定的单词与字符。您也可以为文档指定编码。为了防止出现不需要的自动编辑，例如把替换字符插入到不必要的地方，EmEditor 在替换您指定的单词后，不会马上保存变更，并且会高亮所有替换字符。这样，您不仅能确认文档中的变更，也能用 **“撤消”** 命令来及时复原取代之前的文档。

## 请参考