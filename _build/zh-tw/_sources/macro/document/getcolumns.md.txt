# GetColumns 方法

在 CSV 模式中檢索欄數。如果文檔不是 CSV 模式，那么該方法會返回 0。

#### \[JavaScript\]

_n_ = document. **GetColumns**();

#### \[VBScript\]

_n_ = document. **GetColumns**()

## 版本

支持 EmEditor Professional 14.7 或之後的版本。