# Save 方法

儲存目前的組態。

#### \[JavaScript\]

object. **Save**( \[ _strName_ \] );

#### \[VBScript\]

object. **Save** \[ _strName_ \]

## 參數

_strName_

可選。指定要儲存的組態名稱。如果省略，名稱將不會更改。

## 版本

支持 EmEditor 7.00 或之後的版本。