# Q. 為什么「層疊」或「平鋪」命令不起作用？

當「啟用標籤」被選擇時，選擇 **「層疊」** 命令或任何 **「平鋪」** 命令會讓標籤頁不能用。這些命令只能用在當許多群組存在時。因此，這些命令沒有任何意義當只有一個群組存在時。這個設定是來自于 EmEditor 使用者的要求。要像之前那樣層疊或平鋪，請先禁用標籤頁，然后再選擇命令。即使只有一個群組存在，您還是可以用 **「新增水平群組」** 和 **「新增垂直群組」** 這兩個命令來平鋪視窗。