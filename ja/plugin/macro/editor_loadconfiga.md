# Editor\_LoadConfigA

ANSI文字列で指定する設定を読み直します。このインライン関数を使うか、または [EE\_LOAD\_CONFIGA メッセージ](../message/ee_load_configa) を直接送ることができます。

Editor\_LoadConfigA( HWND hwnd, LPCSTR szConfigName );

## パラメータ

_hwnd_

EmEditor ビューまたはフレームのウィンドウ ハンドルを指定します。

_szConfigName_

設定の名前を指定します。

## 戻り値

戻り値は利用されません。
