# KeepRunning プロパティ (Shell オブジェクト)

V8 を使用中、マクロを継続して実行するかどうかのフラグを取得または設定します。

## 

### \[JavaScript\]

```
b = shell.KeepRunning;
shell.KeepRunning = b;
```

### \[VBScript\]

```
b = shell.KeepRunning
shell.KeepRunning = b
```

## バージョン

EmEditor Professional Version 23.1 以上で利用できます。
