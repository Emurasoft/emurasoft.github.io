# scrollTo メソッド (Window オブジェクト)

スクロール バーを指定する位置に移動します。

## 

### \[JavaScript\]

```
scrollTo( x, y);
```

### \[VBScript\]

```
scrollTo x, y
```

## パラメータ

_x_

水平方向に移動先の桁数を指定します。

_y_

垂直方向に移動先の行数を指定します。

## バージョン

EmEditor Professional Version 4.00 以上で利用できます。
