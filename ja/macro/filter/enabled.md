# Enabled プロパティ (Filter オブジェクト)

オブジェクトが有効どうか示すフラグを指定します。

## 

### \[JavaScript\]

```
b = item.Enabled;
item.Enabled = b;
```

### \[VBScript\]

```
b = item.Enabled
item.Enabled = b
```

## バージョン

EmEditor Professional Version 16.0 以上で利用できます。
