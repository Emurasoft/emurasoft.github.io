# Editor\_GetRedraw

检索在 EmEditor 中允许或禁止重绘变更的标志。你能直接用该内联函数或明确地发送
[EE\_GET\_REDRAW](../message/ee_get_redraw) 消息。

Editor\_GetRedraw( HWND hwnd );

## 参数

_hwnd_

> 指定 EmEditor 视图或框架的窗口句柄。

## 返回值

> 不使用返回值。

## 支持版本

> 支持 EmEditor 5.00 或之后的版本。