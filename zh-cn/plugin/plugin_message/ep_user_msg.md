# EP\_USER\_MSG

发送插件的用户定义消息。

EP\_USER\_MSG

hwnd = hwndView;

wParam = wParam;

lParam = lParam;

## 参数

_hwndView_

EmEditor 视图的窗口句柄。

_wParam_

第一个参数。该参数的含义取决于插件。

_lParam_

第二个插件。该参数的含义取决于插件。

## 返回值

返回值的含义取决于该插件。

## 版本

支持 Version 15.5 或之后的版本。
