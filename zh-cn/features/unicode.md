# 支持 Unicode

EmEditor 支持 Unicode，一种支持多语言环境的编码方式。您不仅可以打开 Unicode 文件，而且还可以用 Unicode 编辑文件来同时显示多种语言。

EmEditor 支持 _低位优先 Unicode，UTF-8，UTF-7，波罗的文，_
_中欧字符，简体中文，繁体中文，西里尔文，希腊文，_
_日文（Shift-JIS），日文（JIS），日文（EUC），韩文，泰文，土耳其文，越南文，西欧语系_ 以及所有其他可用于 Windows 的编码。

## 注意：

EmEditor 不支持使用从右到左的脚本语言，例如阿拉伯语和希伯来语。