# 获取一个选定的字符串

要获取一个选定的字符串，用 [Text 属性](../selection/selection_text)。

下面的例子演示了如何获得并显示一个被选取的字符串。

#### \[JavaScript\]

str = document.selection.Text;

alert( str );

#### \[VBScript\]

str = document.selection.Text

alert str