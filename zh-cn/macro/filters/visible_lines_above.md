# VisibleLinesAbove 属性 (Filters ����)

指定匹配行以上可见行的行数。

## 

### \[JavaScript\]

```
n = list.VisibleLinesAbove;
list.VisibleLinesAbove = n;
```

### \[VBScript\]

```
n = list.VisibleLinesAbove
list.VisibleLinesAbove = n
```

## 版本

支持 EmEditor 16.2 或之后的版本。
