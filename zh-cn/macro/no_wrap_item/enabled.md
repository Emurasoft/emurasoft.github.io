# Enabled 属性 (NoWrapItem 对象)

指定表示对象是否被启用的标志。

## 

### \[JavaScript\]

```
b = item.Enabled;
item.Enabled = b;
```

### \[VBScript\]

```
b = item.Enabled
item.Enabled = b
```

## 版本

支持 EmEditor 7.00 或之后的版本。
