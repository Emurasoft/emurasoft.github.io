# regex 属性 (Editor ����)

检索 [**Regex** 对象](../regex/index)。

#### \[JavaScript\]

_reg_ = editor. **regex**;

#### \[VBScript\]

Set _reg_ = editor. **regex**

## 版本

支持 EmEditor Professional Version 15.9 或之后的版本。