# IgnoreCase 属性 (Regex ����)

设置或检索是否正则表达式忽略大小写。

## 

### \[JavaScript\]

```
b = reg.IgnoreCase;
reg.IgnoreCase = b;
```

### \[VBScript\]

```
b = reg.IgnoreCase;
reg.IgnoreCase = b;
```

## 版本

支持 EmEditor Professional Version 15.9 或之后的版本。
