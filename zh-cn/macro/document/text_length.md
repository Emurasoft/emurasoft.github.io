# TextLength 属性（Document 对象）

检索文档文本的长度。如果文档包含超过 0x7ffffffe 个 UTF-16 字符，则该属性会被截断为该长度。

## 

### \[JavaScript\]

```
len = document.TextLength;
```

### \[VBScript\]

```
len = document.TextLength
```

## 版本

支持 EmEditor Professional 26.3 或更高的版本。