# scrollBy 方法（WebBar 对象）

按指定的相对距离滚动窗口。

##

### \[JavaScript\]

```
WebBar.scrollBy( dx, dy );
```

### \[VBScript\]

```
WebBar.scrollBy dx, dy
```

## 参数

_dx_

指定要水平滚动的距离。

_dy_

指定要垂直滚动的距离。

## 版本

支持 EmEditor Professional 26.3 或更高的版本。