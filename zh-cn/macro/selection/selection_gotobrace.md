# GoToBrace 方法 (Selection ����)

把光标移动到相对应的括号处。

#### \[JavaScript\]

document.selection. **GoToBrace**( \[ _bExtend_ \] );

#### \[VBScript\]

document.selection. **GoToBrace** \[ _bExtend_ \]

## 参数

_bExtend_

可选项。指定是否折叠被移动的文本。默认的值为 false，并且所移动的文本被折叠。

## 版本

支持 EmEditor 4.00 或之后的版本。