# QuotationMark 属性 (Csv ����)

检索或设置当前 Csv 对象的引号标记。

#### \[JavaScript\]

_ch_ = object. **QuotationMark**;

object. **QuotationMark** = _ch_;

#### \[VBScript\]

_ch_ = object. **QuotationMark**

object. **QuotationMark** = _ch_

## 版本

支持 EmEditor 20.1 或之后的版本。