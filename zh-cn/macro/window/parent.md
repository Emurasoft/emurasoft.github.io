# Parent 属性 (Window ����)

返回父窗口对象。

#### \[JavaScript\]

_wnd_ = wnd. **Parent**;

wnd. **Parent** = _wnd_;

#### \[VBScript\]

_wnd_ = wnd. **Parent**

wnd. **Parent** = _wnd_

## 版本

支持 EmEditor 7.00 或之后的版本。
