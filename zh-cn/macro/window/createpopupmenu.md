# CreatePopupMenu 方法 (Window ����)

创建一个弹出菜单。

#### \[JavaScript\]

_popupmenu_ = **CreatePopupMenu**();

#### \[VBScript\]

Set _popupmenu_ = **CreatePopupMenu**

## 返回值

返回一个新创建的 PopupMenu 对象。

## 版本

支持 EmEditor 5.00 或之后的版本。
