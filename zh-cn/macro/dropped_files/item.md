# Item 属性

检索指定索引下的被拖放的文件名。

#### \[JavaScript\]

_str_ = DroppedFiles. **Item**( _Index_ );

#### \[VBScript\]

_str_ = DroppedFiles. **Item**( _Index_ )

## 参数

_Index_

把被拖放的文件索引指定为以 1 为基准的整数。

## 版本

支持 EmEditor 8.00 或之后的版本。