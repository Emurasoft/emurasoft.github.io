# Reset 方法 (Config ����)

重设当前配置。

#### \[JavaScript\]

object. **Reset**( \[ _strName_ \] );

#### \[VBScript\]

object. **Reset** \[ _strName_ \]

## 参数

_strName_

可选。指定要重设的默认配置名称。如果省略，默认配置的当前名称会被使用。如果名称不是默认配置之一，会出现错误。

## 版本

支持 EmEditor 7.00 或之后的版本。
