# Load 方法

加载一个配置。

#### \[JavaScript\]

object. **Load**( \[ _strName_ \] );

#### \[VBScript\]

object. **Load** \[ _strName_ \]

## 参数

_strName_

可选。指定要被加载的配置名称。如果省略，名称将不会更改。

## 版本

支持 EmEditor 7.00 或之后的版本。