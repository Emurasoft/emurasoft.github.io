# GetText 方法 (PopupMenu ����)

检索被标识符指定的菜单项目的文本字符串。

#### \[JavaScript\]

_strText_ = popupmenu. **GetText**( _id_ );

#### \[VBScript\]

_strText_ = popupmenu. **GetText**( _id_ )

## 参数

_id_

菜单项目的整数标识符由 [Add 方法](add) 指定。

## 版本

支持 EmEditor 5.00 或之后的版本。