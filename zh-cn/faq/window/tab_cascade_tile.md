# Q. 为什么“层叠”或“平铺”命令不起作用？

当“启用标签”被选择时，选择 **“层叠”** 命令或任何 **“平铺”** 命令会让标签页不能用。这些命令只能用在当许多群组存在时。因此，这些命令没有任何意义当只有一个群组存在时。这个设定是来自于 EmEditor 用户的要求。要像之前那样层叠或平铺，请先禁用标签页，然后再选择命令。即使只有一个群组存在，您还是可以用 **“新建水平群组”** 和 **“新建垂直群组”** 这两个命令来平铺窗口。
