# Q. 怎么知道每行的换行方式（例如，CR,LF,CR+LF）？

请在“自定义”对话框中的“状态栏”页面上勾选“光标处字符的字符代码”。当光标在任一行的行末处时，您会看到 EmEditor 窗口中的状态栏上分别显示0D+0A（CR+LF），或0D（仅仅CR）或0A（仅仅LF）。
另外，您可以到配置属性中的“标记”页面上勾选“使用不同的标记来显示CR与LF”来标示不同的换行方式。由于在 EmEditor 中每一行都可以有不同的换行方式，如果您勾选了这个选项，EmEditor会显示每一行的换行方式。
