# “检测全部结果”对话框

当一个文件在启用全部检测选项的情况下被打开，EmEditor 就会显示这个对话框。你可以指定一个要用来打开文件的编码。

### “编码”列表框

为当前文件显示检测到的编码列表。

### “预览”文本框

用选取的编码预览被编码的文件。

### “打开 US-ASCII 作为系统默认编码”复选框

当选取的编码是 US-ASCII 时，这个选项会把文件作为系统默认编码打开。
